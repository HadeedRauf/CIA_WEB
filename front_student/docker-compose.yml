version: "3"

services:
  front:
    build:
      context: .
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL:-localhost:3000}
    ports:
      - "${FRONT_BIND_IP:-127.0.0.1}:8080:80"
#      PUBLIC_URL: "localhost:8080"
    restart: always